import mutations from '@/store/modules/table.js'
import data from '@/store/data.json'
import _ from 'lodash'

describe('SET_CURRENT_PAGE', () => {
     it('Sets the inital page', () => {
          console.log(data)
          console.log(mutations)

     })
})